document.addEventListener("DOMContentLoaded", () => { const type = document.querySelector("#booking-type"); const number = document.querySelector("#booking-bed"); populateBedNumbers(type, number); const params = new URLSearchParams(window.location.search); if (params.get("type")) { type.value = params.get("type"); type.dispatchEvent(new Event("change")); } if (params.get("bed")) number.value = params.get("bed"); document.querySelector("#booking-form").addEventListener("submit", (event) => { event.preventDefault(); document.querySelector("#booking-status").textContent = "Thanks. Your enquiry is ready to be reviewed by Sinhassan 104."; }); });